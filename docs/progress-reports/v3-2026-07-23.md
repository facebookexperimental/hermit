# Hermit Progress Report v3 — 2026-07-23

Generated by `scripts/progress-report.sh`. All numbers are live measurements.
Suites that cannot run are recorded with the exact reason (see rubric:
`.llms/skills/progress-rubric.md`).

## Test context

| Field | Value |
| --- | --- |
| Commit tested | `2599e1efdbbc223fb8cd83b5df02253fc371dbe6` (`2599e1e`) |
| Branch | main (pull: Already up to date.) |
| Date (UTC) | 2026-07-23 |
| Backend | ptrace |
| Host CPU | AMD EPYC 9D85 158-Core Processor |
| Kernel | 6.17.13-0_fbk0_crackerjackhost_0_g2b4321c50d79 |
| perf_event_paranoid | 1 |
| Toolchain | rustc 1.99.0-nightly (be8e82435 2026-07-11); cargo 1.99.0-nightly (59800466c 2026-07-07); cargo-nextest 0.9.140 (a9fef2964 2026-07-05) |
| Guest runtimes | python3 node redis-server sqlite3 java  |

## Host limitations encountered

Observed facts on this host (AMD EPYC 9D85, kernel
`6.17.13-0_fbk0_crackerjackhost_0_g2b4321c50d79`):

- **CPUID faulting / PMU: available.** The working-envelope vector ran real
  `hermit run` at L1–L4 (L3 adds `--detlog-heap --detlog-stack`; L4 = 20 reps)
  and passed 3/3 at every level with no `Unable to intercept CPUID` warning in
  `/tmp/progress-envelope.log`. `perf_event_paranoid=1`. This host does **not**
  reproduce the CPUID-faulting gap reported on the previous VM (v3-2026-07-22).
- **No missing guest runtimes.** `python3 node redis-server sqlite3 java` are all
  present. `language_runtime_determinism` still reports 0/0/6 because its cases
  are `#[ignore]` pending the `-- --ignored` toolchain matrix, not because a
  runtime is missing.
- **No `rr` submodule / no Buck rr matrix** (unchanged, by design — see rr suite
  section).

## Summary table

| Suite | Command | Result |
| --- | --- | --- |
| Strict / fail-closed | scripts/test-fail-closed.sh | ABORTED at exit 1 during inventory (0 tests run): `app_strict_verify/curl_version_is_deterministic_under_strict_verify` is ignored but not allowlisted (see Known blockers) |
| Working-envelope L1-L4+rr | validate.sh --envelope-only | {"l1_pass":3,"l2_pass":3,"l3_pass":3,"l4_pass":3,"rr_pass":3,"total":3,"commit":"2599e1e","l4_reps":20,"probes":[{"probe":"true","l1":1,"l2":1,"l3":1,"l4":1,"rr":1},{"probe":"echo","l1":1,"l2":1,"l3":1,"l4":1,"rr":1},{"probe":"date","l1":1,"l2":1,"l3":1,"l4":1,"rr":1}]} |
| Record/replay | cargo test -p hermit --test record_replay | 24 passed, 0 failed, 0 ignored |
| App: sqlite_veryquick | cargo test -p hermit --test sqlite_veryquick | 1 passed, 0 failed, 1 ignored |
| App: redis_strict | cargo test -p hermit --test redis_strict | 3 passed, 0 failed, 1 ignored |
| App: python_stdlib | cargo test -p hermit --test python_stdlib | 1 passed, 0 failed, 1 ignored |
| App: language_runtime_determinism | cargo test -p hermit --test language_runtime_determinism | 0 passed, 0 failed, 6 ignored |

## rr suite

No `rr_suite` Cargo target and no `third-party/rr` submodule exist in the OSS
repo; Meta's Buck rr matrix is not ported. OSS rr coverage = working-envelope rr
probes + the record_replay target above.

## Recently landed PRs

#215, #218, #221, #220, #224, #223

## Known blockers / follow-ups

1. **Strict / fail-closed ratchet aborts during inventory (repo-config gap, not
   a host limitation or a determinism regression).** Verbatim, from
   `/tmp/progress-strict.log`:

   ```
   fail-closed ratchet: app_strict_verify/curl_version_is_deterministic_under_strict_verify is ignored but is not in /home/newton/work/dev-hermit/hermit/hermit-cli/tests/fail_closed_allowed_ignores.tsv
   ```

   Root cause: the `app_strict_verify` target
   (`hermit-cli/tests/app_strict_verify.rs:151`) defines
   `curl_version_is_deterministic_under_strict_verify` with `#[ignore]`, but that
   test is absent from
   `hermit-cli/tests/fail_closed_allowed_ignores.tsv` (25 entries; no `curl`
   row). `scripts/test-fail-closed.sh` is fail-fast (`set -e`) and rejects any
   ignored test not on the allowlist during the inventory phase, so it exits 1
   **before running any test** (0 enabled tests executed). This is new since
   v3-2026-07-22 (the `app_strict_verify` target did not exist then).
   Follow-up: either add the `curl_version_...` row to
   `fail_closed_allowed_ignores.tsv` or un-ignore/gate the test, then re-run the
   ratchet. Until then the ratchet cannot certify the enabled fail-closed set,
   and it also no longer reaches the previously-observed
   `clock_determinism/strict_mode_eliminates_native_clock_nondeterminism` host
   case.

2. **`language_runtime_determinism` = 0 passed / 0 failed / 6 ignored.** All six
   runtime cases (go/jvm/node/ocaml/python/ruby) are `#[ignore]`; they require
   `-- --ignored` and the toolchain matrix. Not a failure — just uncovered by the
   default run. Follow-up: run the `--ignored` matrix on a runner with the
   toolchains to get live numbers.

3. **App suites carry 1 ignored case each** (`sqlite_veryquick`, `redis_strict`,
   `python_stdlib`): the extended source-build / large-download variants remain
   `#[ignore]`. The active non-ignored cases all pass (see summary table).

Note (context, not measured here): today's landed work includes the record/replay
pipe-deadlock and poll-retry investigations and DBI/KVM architecture audits; the
record_replay suite grew to 24 passing (was 17 on 2026-07-22).

## Logs

- Strict: `/tmp/progress-strict.log`
- Envelope: `/tmp/progress-envelope.log`
- Record/replay: `/tmp/progress-record.log`
- Apps: `/tmp/progress-apps.log`

> If the strict ratchet aborted, it is fail-fast: later targets did not run.
> Review the log and classify each failure as host limitation vs product
> regression before publishing.
