schema: 3
bucket: backend-parity-c
test:
  - id: backend-parity-c/aio-refusal
    description: Strict verification for tests/backend-parity/fixtures/aio_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/aio_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/set-tid-address
    description: Strict verification for tests/backend-parity/fixtures/set_tid_address.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/set_tid_address.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/personality-domain
    description: Fail-closed execution-domain round-trip via personality(2)
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/personality_domain.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
        backends_enabled:
          - ptrace
          - liteinst
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: DBT completes the five-step contract but its dedicated adapter publishes typed no_result with no INFO comparison, so it cannot establish L2
          kvm: Current KVM fixture and /bin/true controls time out before guest start on the validation host, so no personality-specific verdict is available
          sabre: Not yet qualified for the personality(2) execution-domain round-trip
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: Native Linux is used as a focused correctness oracle; repeated native execution is not a nondeterminism contract
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the strict personality-domain contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: The process-local personality round-trip has no schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No additional personality-domain edge-case arguments are calibrated
          dbt: No additional personality-domain edge-case arguments are calibrated
          kvm: No additional personality-domain edge-case arguments are calibrated
          sabre: No additional personality-domain edge-case arguments are calibrated
          liteinst: No additional personality-domain edge-case arguments are calibrated
  - id: backend-parity-c/cachestat-refusal
    description: Strict verification for tests/backend-parity/fixtures/cachestat_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/cachestat_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/child-subreaper-refusal
    description: Strict verification for tests/backend-parity/fixtures/child_subreaper_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/child_subreaper_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/close-range-fds
    description: Strict verification for tests/backend-parity/fixtures/close_range_fds.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/close_range_fds.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/copy-file-range-refusal
    description: Strict verification for tests/backend-parity/fixtures/copy_file_range_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/copy_file_range_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/cpuid-probe
    description: Strict verification for tests/backend-parity/fixtures/cpuid_probe.c
    lane: privileged
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cpuid
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/cpuid_probe.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: Initial C-corpus migration preserves the established ptrace baseline; qualify DBT separately
          sabre: SaBRe does not virtualize CPUID; this oracle requires Hermit's synthetic CPU identity
          liteinst: Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/epoll-pwait2
    description: Strict verification for tests/backend-parity/fixtures/epoll_pwait2_readiness.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/epoll_pwait2_readiness.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns deterministic ENOSYS for epoll_pwait2 (syscall 441); it implements the classic epoll_wait but not the newer wait entry point
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/epoll-readiness
    description: Strict verification for tests/backend-parity/fixtures/epoll_readiness.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/epoll_readiness.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/faccessat2-flags
    description: Strict verification for tests/backend-parity/fixtures/faccessat2_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/faccessat2_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fadvise-hints
    description: Strict verification for tests/backend-parity/fixtures/fadvise_hints.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fadvise_hints.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fallocate-extents
    description: Strict verification for tests/backend-parity/fixtures/fallocate_extents.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fallocate_extents.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fchmod-bits
    description: Strict verification for tests/backend-parity/fixtures/fchmod_bits.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fchmod_bits.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/file-backed-mmap
    description: Strict verification for tests/backend-parity/fixtures/file_backed_mmap.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/file_backed_mmap.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/flock-lifecycle
    description: Strict verification for tests/backend-parity/fixtures/flock_lifecycle.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/flock_lifecycle.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fsync-durability
    description: Strict verification for tests/backend-parity/fixtures/fsync_durability.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fsync_durability.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor forwards fsync and fdatasync but returns deterministic ENOSYS for syncfs
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/getrusage-self-accounting
    description: Strict verification for tests/backend-parity/fixtures/getrusage_self_accounting.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/getrusage_self_accounting.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: false
        ci_disabled_reason:
          liteinst: {"result": "determinism-failure", "evidence": "8f6d8a57d6a8e4ef7fa0ae14c16e2a494a2c0ab5; task ov-backend-ratchet-fanout", "reason": "Exact-head pipe-capacity canonical verification diverged with its first divergence at a /proc/self/maps COMMIT (turn 10), matching three prior LiteInst demotions; durable population evidence measured 20 of 26 LiteInst candidates diverging at that shared startup point and later saw 2 of the 6 apparent PASSx3 survivors fail, so a single matching pair does not qualify this verify population."}
        backends_enabled:
          - ptrace
          - liteinst
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify KVM separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/inotify-watch
    description: Strict verification for tests/backend-parity/fixtures/inotify_watch.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/inotify_watch.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns a deterministic error for the inotify family (inotify_init1), so the filesystem-watch descriptor lifecycle is unavailable
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/ioctl-fionread
    description: Strict verification for tests/backend-parity/fixtures/ioctl_fionread.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/ioctl_fionread.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns ENOTTY for the FIONREAD pipe ioctl; FIONBIO is supported
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/kcmp-refusal
    description: Strict verification for tests/backend-parity/fixtures/kcmp_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/kcmp_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/linkat-flags
    description: Strict verification for tests/backend-parity/fixtures/linkat_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/linkat_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mce-kill-refusal
    description: Strict verification for tests/backend-parity/fixtures/mce_kill_policy.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mce_kill_policy.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/memfd-create
    description: Strict verification for tests/backend-parity/fixtures/memfd_create.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/memfd_create.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mempolicy-default
    description: Strict verification for tests/backend-parity/fixtures/mempolicy_default.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mempolicy_default.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mincore-residency
    description: Strict verification for tests/backend-parity/fixtures/mincore_residency.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mincore_residency.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mkdir-rmdir
    description: Strict verification for tests/backend-parity/fixtures/mkdir_rmdir.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mkdir_rmdir.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mknod-special
    description: Strict verification for tests/backend-parity/fixtures/mknod_special.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mknod_special.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/numa-node-identity
    description: Strict verification for tests/c/numa_node_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/numa_node_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/name-to-handle-refusal
    description: Strict verification for tests/backend-parity/fixtures/name_to_handle.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/name_to_handle.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/no-new-privs-refusal
    description: Strict verification for tests/backend-parity/fixtures/no_new_privs_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/no_new_privs_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/openat2-refusal
    description: Strict verification for tests/backend-parity/fixtures/openat2_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/openat2_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/path-file-ops
    description: Strict verification for tests/backend-parity/fixtures/path_file_ops.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/path_file_ops.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pid-probe
    description: Strict verification for tests/backend-parity/fixtures/pid_probe.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pid_probe.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - sabre
          - liteinst
          - kvm
        backends_disabled:
          dbt: Initial C-corpus migration preserves the established ptrace baseline; qualify DBT separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pipe2-flags
    description: Strict verification for tests/backend-parity/fixtures/pipe2_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pipe2_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/prctl-pdeathsig
    description: Strict verification for tests/backend-parity/fixtures/prctl_pdeathsig.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/prctl_pdeathsig.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns deterministic ENOSYS for prctl PR_SET_PDEATHSIG/PR_GET_PDEATHSIG; ptrace and DBT drive the full parent-death-signal state machine
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/preadv2-flags
    description: Strict verification for tests/backend-parity/fixtures/preadv2_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/preadv2_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor does not implement preadv2/pwritev2 (syscalls 327/328); it returns ENOSYS, mirroring its missing classic pwritev/preadv
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pthread-lifecycle
    description: Strict verification for tests/backend-parity/fixtures/pthread_lifecycle.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pthread_lifecycle.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          sabre: false
          kvm: false
        ci_disabled_reason:
          sabre: {"result": "determinism-failure", "evidence": "Generated scorecard ci/compat-envelope/cells.json at Hermit 5b811757980b9cab599206c047560a7962176a6b retains historical results determinism-failure at Hermit 90e46cba9ad83550205f3c9cd232ae222c6b6173; retained first divergent record positions: 26, 26, 26.", "reason": "Historical canonical verification diverged at recorded positions. The later clean observations do not erase that retained failure, so this backend remains excluded pending separate qualification."}
          kvm:
            result: timeout
            evidence: RUN1709 at Hermit 8c2bb5620f13e5d351e7bdb5d20c8dcec1f3eaba
            reason: Both canonical full-validation attempts exceeded the 57-second wall backstop and produced no comparison, so KVM remains red without a divergence claim.
        backends_enabled:
          - ptrace
          - sabre
          - kvm
        backends_disabled:
          dbt: Initial C-corpus migration preserves the established ptrace baseline; qualify DBT separately
          liteinst: Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/readdir-entries
    description: Strict verification for tests/backend-parity/fixtures/readdir_entries.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/readdir_entries.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/rename-ops
    description: Strict verification for tests/backend-parity/fixtures/rename_ops.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/rename_ops.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/renameat2-flags
    description: Strict verification for tests/backend-parity/fixtures/renameat2_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/renameat2_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/robust-list
    description: Strict verification for tests/backend-parity/fixtures/robust_list.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/robust_list.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/seccomp-refusal
    description: Strict verification for tests/backend-parity/fixtures/seccomp_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/seccomp_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/signalfd-create
    description: Strict verification for tests/backend-parity/fixtures/signalfd_create.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/signalfd_create.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/socket-epoll-ordering
    description: 'Contract fixture: ephemeral-port selection and epoll readiness ORDERING; the guest branches on which fd is ready first, so the divergence propagates into the syscall sequence rather than a printable set. Loopback only.'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/socket_epoll_ordering.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: Never measured for this cell; the original .toml cited a missing toolchain on the authoring host, which is not evidence about the backend
          sabre: Never measured for this cell; the original .toml cited a missing toolchain on the authoring host, which is not evidence about the backend
          liteinst: Never measured for this cell; the original .toml cited a missing toolchain on the authoring host, which is not evidence about the backend
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/statfs-free-determinism
    description: Strict verification for tests/backend-parity/fixtures/statfs_free_determinism.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/statfs_free_determinism.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/statx-metadata
    description: Strict verification for tests/backend-parity/fixtures/statx_metadata.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/statx_metadata.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/symlink-ops
    description: Strict verification for tests/backend-parity/fixtures/symlink_ops.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/symlink_ops.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/sync-file-range
    description: Strict verification for tests/backend-parity/fixtures/sync_file_range.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/sync_file_range.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/sysv-ipc-refusal
    description: Strict verification for tests/backend-parity/fixtures/sysv_ipc_refusal.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/sysv_ipc_refusal.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/thp-disable
    description: Strict verification for tests/backend-parity/fixtures/thp_disable.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/thp_disable.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns deterministic ENOSYS for prctl PR_SET_THP_DISABLE/PR_GET_THP_DISABLE; ptrace and DBT drive the full transparent-hugepage flag round-trip
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/umask-mode
    description: Strict verification for tests/backend-parity/fixtures/umask_mode.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/umask_mode.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/uname-identity
    description: Strict verification for tests/backend-parity/fixtures/uname_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/uname_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT forwards the host uname(2) nodename instead of pinning it; ptrace and KVM determinize the hostname, DBT leaks the real host name
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/utimensat-determinism
    description: Strict verification for tests/backend-parity/fixtures/utimensat_determinism.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/utimensat_determinism.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/socket-options
    description: Strict verification for tests/backend-parity/fixtures/socket_options.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/socket_options.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor accepts setsockopt but getsockopt returns ENOPROTOOPT for SO_REUSEADDR/SO_KEEPALIVE/SO_BROADCAST, so the option round-trip observes ok=0 instead of ok=6
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/shutdown-socketpair
    description: Strict verification for tests/backend-parity/fixtures/shutdown_socketpair.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/shutdown_socketpair.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/sockname-unnamed
    description: Strict verification for tests/backend-parity/fixtures/sockname_unnamed.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/sockname_unnamed.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor implements getsockname but returns ENOSYS for getpeername on an AF_UNIX socket pair, so the peer-name checks fail and it observes ok=4 instead of ok=6
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/bind-getsockname
    description: Strict verification for tests/backend-parity/fixtures/bind_getsockname.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/bind_getsockname.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/msync-writeback
    description: Strict verification for tests/backend-parity/fixtures/msync_writeback.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/msync_writeback.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM msync returns 0 but its in-memory mapping model does not flush MAP_SHARED writes to the backing file, so the pread write-through checks fail and it observes ok=3 instead of ok=5
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/getpriority-identity
    description: Strict verification for tests/c/getpriority_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/getpriority_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/sendfile-copy
    description: Strict verification for tests/backend-parity/fixtures/sendfile_copy.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/sendfile_copy.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/poll-readiness
    description: Strict verification for tests/backend-parity/fixtures/poll_readiness.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/poll_readiness.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor personality implements poll/ppoll readiness but returns deterministic ENOSYS for select(2)/pselect6
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/append-pwrite
    description: Strict verification for tests/backend-parity/fixtures/append_pwrite.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/append_pwrite.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/prctl-identity
    description: Strict verification for tests/c/prctl_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/prctl_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
        backends_enabled:
          - ptrace
          - dbt
        backends_disabled:
          kvm: 'inherited from the L1 KVM prctl_identity gap: PR_SET_NAME returns ENOSYS so the guest exits non-zero and there is no L2 run to verify'
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/rlimit-identity
    description: Strict verification for tests/c/rlimit_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/rlimit_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pipe-ipc
    description: Strict verification for tests/backend-parity/fixtures/pipe_ipc.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pipe_ipc.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/vectored-io
    description: Strict verification for tests/backend-parity/fixtures/vectored_io.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/vectored_io.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/eventfd-semantics
    description: Strict verification for tests/backend-parity/fixtures/eventfd_probe.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/eventfd_probe.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/ftruncate-sparse
    description: Strict verification for tests/backend-parity/fixtures/ftruncate_sparse.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/ftruncate_sparse.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/vectored-file-io
    description: Strict verification for tests/backend-parity/fixtures/vectored_file_io.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/vectored_file_io.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns deterministic ENOSYS for pwritev/preadv (positioned vectored I/O); plain writev/readv are supported but the positioned variants are not
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/openat-flags
    description: Strict verification for tests/backend-parity/fixtures/openat_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/openat_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fchmodat2-flags
    description: Strict verification for tests/backend-parity/fixtures/fchmodat2_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fchmodat2_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns ENOSYS for fchmodat2 (syscall 452); ptrace and DBT forward it to the host
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/socketpair-flags
    description: Strict verification for tests/backend-parity/fixtures/socketpair_flags.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/socketpair_flags.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor getsockopt implements SO_TYPE but returns ENOPROTOOPT for SO_DOMAIN and SO_ACCEPTCONN, so the socket-introspection contract observes ok=5 instead of ok=7
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pidfd-open-self
    description: Strict verification for tests/backend-parity/fixtures/pidfd_open_self.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pidfd_open_self.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/cpu-virtualization
    description: Strict verification for tests/backend-parity/fixtures/cpu_virtualization.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/cpu_virtualization.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/getcpu-identity
    description: Strict verification for tests/c/getcpu_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/getcpu_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/sched-getaffinity-identity
    description: Strict verification for tests/c/sched_getaffinity_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/sched_getaffinity_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fd-duplication
    description: Strict verification for tests/backend-parity/fixtures/fd_duplication.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fd_duplication.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        ci_disabled_reason:
          dbt: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained a determinism failure; this cell did not pass every repetition cleanly. Retained detail: canonical verification did not match: verified=false verdict=diverged bitwise_parity=false", "result": "determinism-failure"}
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/dup-shared-offset
    description: Strict verification for tests/backend-parity/fixtures/dup_shared_offset.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/dup_shared_offset.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        ci_disabled_reason:
          dbt: {"result": "determinism-failure", "evidence": "Published parent series at dev-hermit 90a55d8190dd2551179a9394be3504ee89a6a5a9 retains pressure-test results pass and determinism-failure for Detcore tree 077a5d847f6541daa5ef145536a7065868e9d3ab; first divergent record 80 and syscall 32.", "reason": "Published canonical verification at the same Detcore tree contains both passing and divergent observations. The 20 clean repetitions in this campaign do not erase the retained divergence, so this backend remains excluded pending separate qualification."}
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/lseek-positioning
    description: Strict verification for tests/backend-parity/fixtures/lseek_positioning.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/lseek_positioning.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
        backends_disabled:
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
        ci_disabled_reason:
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pipe-capacity
    description: Strict verification for tests/backend-parity/fixtures/pipe_capacity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pipe_capacity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pipe-capacity-pin
    description: Detcore-created pipes expose the fixed 8192-byte scheduler capacity instead of the host default
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pipe_capacity_pin.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: Canonical DBT verification has not been qualified for this environment-control fixture
          sabre: Canonical SaBRe verification has not been qualified for this environment-control fixture
          liteinst: LiteInst canonical verification remains blocked by guest-visible startup mapping identity
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (by design): native Linux exposes the host pipe default, while this fixture asserts Detcore policy.'
        backends_enabled: []
        backends_disabled:
          native: Native Linux intentionally exposes the host-dependent default rather than the Detcore pin
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: record/replay qualification is separate from the initial strict-verification regression cell.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from this strict-verification regression cell
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (by design): this fixed-value environment oracle has no schedule-diversity contract.'
        backends_enabled: []
        backends_disabled:
          ptrace: A fixed-value environment oracle has no schedule-diversity contract
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: no custom edge-case arguments are needed for the exact 8192-byte contract.'
        backends_enabled: []
        backends_disabled:
          ptrace: The exact 8192-byte contract needs no custom arguments
          dbt: The exact 8192-byte contract needs no custom arguments
          kvm: The exact 8192-byte contract needs no custom arguments
          sabre: The exact 8192-byte contract needs no custom arguments
          liteinst: The exact 8192-byte contract needs no custom arguments
  - id: backend-parity-c/record-lock
    description: Strict verification for tests/backend-parity/fixtures/record_lock.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/record_lock.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
        backends_enabled:
          - ptrace
          - liteinst
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor implements F_SETLK advisory locking but returns a deterministic ENOSYS for F_GETLK lock queries
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/cwd-roundtrip
    description: Strict verification for tests/backend-parity/fixtures/cwd_roundtrip.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/cwd_roundtrip.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/environment-and-workdir
    description: The scheduled guest sees exactly the minimal environment and starts in a fresh tmpfs at /test
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/environment_and_workdir.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        guest_args:
          ptrace:
            - workdir
          kvm:
            - workdir
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: DBT does not enter the per-cell /test mount namespace
          kvm: KVM verification does not yet compare canonical non-vacuous INFO evidence
          sabre: Canonical SaBRe verification has not been qualified for this environment-control fixture
          liteinst: LiteInst canonical verification remains blocked by guest-visible startup mapping identity
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: native execution cannot apply Hermit environment or mount controls.'
        backends_enabled: []
        backends_disabled:
          native: Native execution cannot apply Hermit environment or mount controls
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: record/replay qualification is separate from these scheduled run controls.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from these scheduled run controls
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: fixed environment and workdir values have no schedule-diversity contract.'
        backends_enabled: []
        backends_disabled:
          ptrace: Fixed environment and workdir values have no schedule-diversity contract
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: true
        args:
          - --strict
        guest_args:
          ptrace:
            - environment
        assert:
          runs: 3
          repeat_identical: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: DBT does not enter the per-cell /test mount namespace
          kvm: KVM custom execution is not qualified by the canonical comparison policy
          sabre: SaBRe custom execution is not qualified for this environment-control fixture
          liteinst: LiteInst custom execution remains blocked by guest-visible startup mapping identity
  - id: backend-parity-c/o-tmpfile-anon
    description: Strict verification for tests/backend-parity/fixtures/o_tmpfile_anon.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/o_tmpfile_anon.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fcntl-owner
    description: Strict verification for tests/backend-parity/fixtures/fcntl_owner.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fcntl_owner.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
        backends_enabled:
          - ptrace
          - liteinst
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          kvm: KVM ElfExecutor returns a deterministic error for the F_SETOWN/F_GETOWN async-I/O owner fcntl family, so the SIGIO/SIGURG owner round-trip is unavailable
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/membarrier-query
    description: Strict verification for tests/backend-parity/fixtures/membarrier_query.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/membarrier_query.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: L1 parity established in the source matrix row; the L2 --verify witness was not recorded, so qualify DBT separately
          sabre: Not evaluated in the source backend-parity matrix; qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/stat-metadata-identity
    description: 'Contract fixture: stat/fstat/lstat/statx metadata (st_ino, st_dev, timestamps incl. statx btime, st_nlink, st_blocks) does not leak host state; timestamps come from continuous virtual time (identical double-run AND still advancing within a run); includes a branch on an inode comparison.'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/stat_metadata_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: Not buildable on the authoring host (cmake absent); qualify DBT separately
          sabre: Not buildable on the authoring host (cmake absent); qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (by design): a native run necessarily differs from itself here, since unvirtualized st_ino/st_dev come from the real host filesystem and the timestamps are wall-clock, so native is not a discriminating oracle for a contract whose whole claim is that those fields are virtualized.'
        backends_enabled: []
        backends_disabled:
          native: 'A native run necessarily differs from itself: unvirtualized st_ino/st_dev come from the real host filesystem and the timestamps are wall-clock, so native is a useful smoke check but not a discriminating oracle for a contract whose whole claim is that those fields are virtualized and repeatable'
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from strict verification and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (by design): file metadata is filesystem- and virtual-clock-derived, not schedule-derived, so chaos adds no discriminating power for this single-threaded guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: File metadata is filesystem- and virtual-clock-derived, not schedule-derived; chaos adds no discriminating power for a single-threaded guest
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mmap-layout-pointer-order
    description: Strict verification for tests/backend-parity/fixtures/mmap_layout_pointer_order.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mmap_layout_pointer_order.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: DBI is not buildable on the current host (third-party-backends feature off; DynamoRIO needs cmake, absent), so no witness could be recorded; qualify DBI separately -- this guest is a PRIORITY for DBI once it builds, since layout divergence is exactly what it detects
          sabre: SaBRe is not buildable on the current host (same third-party-backends gate); qualify SaBRe separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Native output is IDENTICAL to Hermit''s here (measured), because the probe is relocation-invariant by construction -- so a native run is a useful sanity check but not a discriminating oracle, and gating on it would assert nothing'
        backends_enabled: []
        backends_disabled:
          native: Native output is IDENTICAL to Hermit's here (measured), because the probe is relocation-invariant by construction -- so a native run is a useful sanity check but not a discriminating oracle, and gating on it would assert nothing
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from strict verification / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Memory layout is allocation-policy derived, not schedule-derived; chaos adds no discriminating power for a single-threaded guest / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Memory layout is allocation-policy derived, not schedule-derived; chaos adds no discriminating power for a single-threaded guest
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/signal-waitstatus-identity
    description: Cross-backend wait-status identity for tests/c/signal_waitstatus_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/signal_waitstatus_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
        - -pthread
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
        ci_disabled_reason:
          dbt: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained an understood infrastructure failure; this cell did not pass every repetition cleanly. Retained detail: verification recorded no comparison at all (verdict=no_result), so there is no canonical INFO evidence to admit", "result": "infrastructure-error"}
        backends_enabled:
          - ptrace
          - dbt
        backends_disabled:
          sabre: 'MEASURED HANG 2026-08-06: prints through the sigkill case then never returns from the SIGILL case (killed at 150s). This is the defect the fixture exists to pin; re-enable when SaBRe signal delivery terminates.'
          kvm: Cannot complete a guest on the current host (startup livelock); no wait-status observation exists to compare
          liteinst: Unmeasured for this guest; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: The native run is the reference this fixture is compared against, not a case it asserts'
        backends_enabled: []
        backends_disabled:
          native: The native run is the reference this fixture is compared against, not a case it asserts
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay of signal-terminated children is not part of this contract'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay of signal-terminated children is not part of this contract
          dbt: Record/replay of signal-terminated children is not part of this contract
          kvm: Record/replay of signal-terminated children is not part of this contract
          sabre: Record/replay of signal-terminated children is not part of this contract
          liteinst: Record/replay of signal-terminated children is not part of this contract
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Wait-status identity is a fixed contract; chaos scheduling adds no signal there'
        backends_enabled: []
        backends_disabled:
          ptrace: Wait-status identity is a fixed contract; chaos scheduling adds no signal there
          dbt: Wait-status identity is a fixed contract; chaos scheduling adds no signal there
          kvm: Wait-status identity is a fixed contract; chaos scheduling adds no signal there
          sabre: Wait-status identity is a fixed contract; chaos scheduling adds no signal there
          liteinst: Wait-status identity is a fixed contract; chaos scheduling adds no signal there
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/hardware-trap-identity
    description: Cross-backend identity of HARDWARE-generated traps for tests/c/hardware_trap_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/c/hardware_trap_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
        ci_disabled_reason:
          dbt: {"result": "determinism-failure", "evidence": "Generated scorecard ci/compat-envelope/cells.json at Hermit 5b811757980b9cab599206c047560a7962176a6b retains historical results determinism-failure at Hermit 90e46cba9ad83550205f3c9cd232ae222c6b6173; retained first divergent record positions: 492, 366, 374.", "reason": "Historical canonical verification diverged at recorded positions. The later clean observations do not erase that retained failure, so this backend remains excluded pending separate qualification."}
        backends_enabled:
          - ptrace
          - dbt
        backends_disabled:
          sabre: 'MEASURED 2026-08-06: does not deliver #UD to a child with no handler installed (child runs past ud2 and exits 99), and then HANGS on the hardware #DE case until the harness timeout. Re-enable when both are fixed.'
          kvm: Cannot complete a guest on the current host (startup livelock); no trap observation exists to compare
          liteinst: Unmeasured for this guest; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: The native run is the reference this fixture is compared against, not a case it asserts'
        backends_enabled: []
        backends_disabled:
          native: The native run is the reference this fixture is compared against, not a case it asserts
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay of hardware-trap termination is not part of this contract'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay of hardware-trap termination is not part of this contract
          dbt: Record/replay of hardware-trap termination is not part of this contract
          kvm: Record/replay of hardware-trap termination is not part of this contract
          sabre: Record/replay of hardware-trap termination is not part of this contract
          liteinst: Record/replay of hardware-trap termination is not part of this contract
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Trap identity is a fixed contract; chaos scheduling adds no signal there'
        backends_enabled: []
        backends_disabled:
          ptrace: Trap identity is a fixed contract; chaos scheduling adds no signal there
          dbt: Trap identity is a fixed contract; chaos scheduling adds no signal there
          kvm: Trap identity is a fixed contract; chaos scheduling adds no signal there
          sabre: Trap identity is a fixed contract; chaos scheduling adds no signal there
          liteinst: Trap identity is a fixed contract; chaos scheduling adds no signal there
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/host-identity
    description: Strict verification for tests/backend-parity/fixtures/host_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/host_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          dbt: false
        ci_disabled_reason:
          dbt: {"result": "unavailable", "evidence": "hermit-cli/src/bin/hermit/run.rs:2163", "reason": "DBT exits successfully after both guest executions, but its dedicated path emits no terminal canonical comparison, leaving verdict=no_result and comparison=null; retain the cell for direct evidence without reporting it green."}
        backends_enabled:
          - ptrace
          - dbt
        backends_disabled:
          kvm: KVM times out before guest start on the validation host, so no host-identity verdict is available; re-enable when the startup livelock is fixed
          sabre: SaBRe loader is not staged in the default build, so the backend reports unavailable rather than a host-identity verdict
          liteinst: Not evaluated in the source backend-parity matrix; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: A native run intentionally FAILS this contract (it sees the real host identity); that is the fixture''s negative control, not a CI cell'
        backends_enabled: []
        backends_disabled:
          native: A native run intentionally FAILS this contract (it sees the real host identity); that is the fixture's negative control, not a CI cell
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from the host-identity contract / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the host-identity contract
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/short-io-split-identity
    description: 'Partial-transfer split pattern over a blocking pipe: completion asserted in-guest, split pinned by stdout comparison'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/short_io_split_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: 'Measured: the guest produces NO stdout and hermit exits 1 under --backend dbi, so the fixture does not complete there. Recorded as an observed gap, not diagnosed by this change'
          sabre: Not yet qualified for the partial-transfer split contract
          liteinst: Not yet qualified for the partial-transfer split contract
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Native is used here as a correctness oracle only: it never splits this transfer (one full write), so repeated native execution is not a split-pattern contract'
        backends_enabled: []
        backends_disabled:
          native: 'Native is used here as a correctness oracle only: it never splits this transfer (one full write), so repeated native execution is not a split-pattern contract'
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from the strict split-pattern contract / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the strict split-pattern contract
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling would perturb the pipe drain rate and therefore the split itself, which is the observable under contract / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling would perturb the pipe drain rate and therefore the split itself, which is the observable under contract
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
  - id: backend-parity-c/event-delivery-ordering
    description: 'inotify coalescing and signalfd delivery order: event streams reported with their operation denominators'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/event_delivery_ordering.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -D_GNU_SOURCE
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
        backends_enabled:
          - ptrace
          - liteinst
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: 'Measured: the guest produces NO stdout and the run is killed at 150s under --backend dbi, so the event-delivery legs never complete there. Observed gap, not diagnosed by this change'
          kvm: KVM does not start the guest on the validation host; the existing inotify_watch entry already records that its ElfExecutor personality omits the inotify family
          sabre: Not yet qualified for inotify/signalfd event delivery
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Measured stable 5/5 natively on this host, so native repetition is not a nondeterminism contract; the value of this entry is cross-backend, not native-vs-hermit'
        backends_enabled: []
        backends_disabled:
          native: Measured stable 5/5 natively on this host, so native repetition is not a nondeterminism contract; the value of this entry is cross-backend, not native-vs-hermit
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification of event delivery is tracked separately / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification of event delivery is tracked separately
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling perturbs the very timing that decides the coalescing under test, so it would change the observable rather than probe it / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling perturbs the very timing that decides the coalescing under test, so it would change the observable rather than probe it
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/fork-exec-pipeline
    description: 'fork+execve pipeline: fd inheritance across an execve image replacement, output captured through a pipe, exit status propagated'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/fork_exec_pipeline.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: New top-of-funnel coverage is established on the ptrace reference first; DBI ratchets against it separately
          sabre: New top-of-funnel coverage is established on the ptrace reference first; SaBRe ratchets against it separately
          liteinst: New top-of-funnel coverage is established on the ptrace reference first; LiteInst ratchets against it separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: This row asserts strict-verify determinism; it does not assert native nondeterminism'
        backends_enabled: []
        backends_disabled:
          native: This row asserts strict-verify determinism; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from strict-verification entry / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict-verification entry
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/signal-delivery-sequence
    description: 'signal sequence: sigaction, sigprocmask block, non-realtime coalescing of a blocked signal, sigpending, unblock delivery'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/signal_delivery_sequence.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
        backends_enabled:
          - ptrace
          - liteinst
        backends_disabled:
          dbt: New top-of-funnel coverage is established on the ptrace reference first; DBI ratchets against it separately
          kvm: New top-of-funnel coverage is established on the ptrace reference first; KVM ratchets against it separately
          sabre: New top-of-funnel coverage is established on the ptrace reference first; SaBRe ratchets against it separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: This row asserts strict-verify determinism; it does not assert native nondeterminism'
        backends_enabled: []
        backends_disabled:
          native: This row asserts strict-verify determinism; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from strict-verification entry / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict-verification entry
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/file-io-roundtrip
    description: 'file lifecycle on one descriptor: write, fsync, lseek, read-back checksum, fstat size, ftruncate, unlink'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/file_io_roundtrip.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci:
          ptrace: true
          liteinst: true
          kvm: true
        backends_enabled:
          - ptrace
          - liteinst
          - kvm
        backends_disabled:
          dbt: New top-of-funnel coverage is established on the ptrace reference first; DBI ratchets against it separately
          sabre: New top-of-funnel coverage is established on the ptrace reference first; SaBRe ratchets against it separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: This row asserts strict-verify determinism; it does not assert native nondeterminism'
        backends_enabled: []
        backends_disabled:
          native: This row asserts strict-verify determinism; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Record/replay qualification is separate from strict-verification entry / Record/replay is unsupported by DBI / Record/replay is unsupported by KVM / Record/replay is unsupported by SaBRe / Record/replay is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict-verification entry
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle / Chaos scheduling is unsupported by DBI / Chaos scheduling is unsupported by KVM / Chaos scheduling is unsupported by SaBRe / Chaos scheduling is unsupported by LiteInst'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED: No custom edge-case arguments have been calibrated for this C guest'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/pipe-multiwriter-ordering
    description: Strict verification for tests/backend-parity/fixtures/pipe_multiwriter_ordering.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/pipe_multiwriter_ordering.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: The DynamoRIO (DBT) backend is not buildable on the current host (third-party-backends feature off; DynamoRIO needs cmake), so no witness could be recorded; qualify DBT separately once it builds
          kvm: KVM livelocks at guest startup on the current host, so no witness could be recorded; qualify KVM separately
          sabre: SaBRe is not buildable on the current host (same third-party-backends gate); qualify SaBRe separately
          liteinst: LiteInst preload runtime libreverie_liteinst.so is not built beside the binary on the current host; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: "NOT-EXERCISED (by design): native variation is the premise measured by this guest, not a stable naked-mode oracle that the guest asserts."
        backends_enabled: []
        backends_disabled:
          native: Native run-to-run variation is the PREMISE of this guest, not an assertion it makes; it is demonstrated in the fixture comment rather than gated here
      replay:
        ci: false
        ci_disabled_reason: "NOT-EXERCISED: record/replay qualification is separate from the initial strict-verification fixture and has not been measured for this guest."
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by the DynamoRIO (DBT) backend
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: "NOT-EXERCISED: the guest has schedule-derived outcomes, but no per-seed chaos oracle has been calibrated, so enabling it would not provide a meaningful verdict."
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos would be a GOOD fit here (this guest has two schedule-derived orderings), but a chaos oracle needs per-seed expected output, which is not calibrated yet
          dbt: Chaos scheduling is unsupported by the DynamoRIO (DBT) backend
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: "NOT-EXERCISED: no custom edge-case arguments or expected outcomes have been calibrated for this guest."
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
  - id: backend-parity-c/mixed-inline-and-libc-syscalls
    description: Patched inline sites and unpatched libc.so syscalls interleaved in one run
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/mixed_inline_and_libc_syscalls.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: Not yet qualified for this guest; ratchet separately
          sabre: Not yet qualified for this guest; ratchet separately
          liteinst: LiteInst preload runtime is not built beside the binary on this host
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Native is the uninstrumented baseline; the contract is the hermit run
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Syscall-site reach is not schedule-derived, so chaos adds no discriminating power
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
  - id: backend-parity-c/inline-syscall-sites
    description: Inline syscall instructions in the main ELF, so a main-ELF rewriter has sites to patch
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/inline_syscall_sites.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: Not yet qualified for this guest; ratchet separately
          sabre: Not yet qualified for this guest; ratchet separately
          liteinst: LiteInst preload runtime is not built beside the binary on this host
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Native is the uninstrumented baseline; the contract is the hermit run
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Syscall-site reach is not schedule-derived, so chaos adds no discriminating power
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
  - id: backend-parity-c/readdir-order-identity
    description: Strict verification for tests/backend-parity/fixtures/readdir_order_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/readdir_order_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        guest_args:
          ptrace:
            - --require-small-determinized
          kvm:
            - --require-small-determinized
        backends_disabled:
          dbt: DBT is not buildable on the current host (third-party-backends feature off; DynamoRIO needs cmake, absent), so no witness could be recorded; qualify DBT separately
          sabre: SaBRe is not buildable on the current host (same third-party-backends gate); qualify SaBRe separately
          liteinst: LiteInst preload runtime libreverie_liteinst.so is not built beside the binary; qualify LiteInst separately
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: 'Native creation-order leakage is the PREMISE of this guest (measured: order_identical=0, seams_rev=n-1), recorded in the fixture header rather than gated here'
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Enumeration order is filesystem-derived, not schedule-derived, so chaos adds no discriminating power here
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
  - id: backend-parity-c/static-nolibc-syscall-sites
    description: 'Freestanding static guest: no PLT, no interpreter, whole program in the main ELF'
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/static_nolibc_syscall_sites.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    build:
      cflags:
        - -static
        - -nostdlib
        - -nostartfiles
    modes:
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: Not yet qualified for this guest; ratchet separately
          sabre: Not yet qualified for this guest; ratchet separately
          liteinst: LiteInst preload runtime is not built beside the binary on this host
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Native is the uninstrumented baseline; the contract is the hermit run
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from strict verification
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Syscall-site reach is not schedule-derived, so chaos adds no discriminating power
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
        ci_disabled_reason: 'NOT-EXERCISED: this mode is outside the measured strict-verification contract for this guest.'
  - id: backend-parity-c/timer-family-identity
    description: Strict verification for tests/backend-parity/fixtures/timer_family_identity.c
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
    occasional: false
    program: tests/backend-parity/fixtures/timer_family_identity.c
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      verify:
        ci: false
        ci_disabled_reason: 'Authoring host could not qualify the non-ptrace backends; the fixture is registered ptrace-only until each is qualified separately.'
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: Not buildable on the authoring host (cmake absent); qualify DBT separately
          kvm: KVM does not complete a guest on the authoring host; qualify KVM separately
          sabre: Not buildable on the authoring host (cmake absent); qualify SaBRe separately
          liteinst: LiteInst preload handshake fails on the authoring host; qualify LiteInst separately
      naked:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide, by design): this migration inventories strict verification and asserts nothing about native nondeterminism, so there is no naked oracle to run.'
        backends_enabled: []
        backends_disabled:
          native: This migration inventories strict verification; it does not assert native nondeterminism
      replay:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): record/replay qualification is a separate exercise from the initial strict-verification migration and has never been run for this guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: Record/replay qualification is separate from the initial strict-verification migration
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): chaos scheduling is only meaningful for a guest with an explicit schedule-diversity oracle, and this guest has none.'
        backends_enabled: []
        backends_disabled:
          ptrace: Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        ci_disabled_reason: 'NOT-EXERCISED (family-wide): no custom edge-case arguments have been calibrated for this C guest.'
        backends_enabled: []
        backends_disabled:
          ptrace: No custom edge-case arguments have been calibrated for this C guest
          dbt: No custom edge-case arguments have been calibrated for this C guest
          kvm: No custom edge-case arguments have been calibrated for this C guest
          sabre: No custom edge-case arguments have been calibrated for this C guest
          liteinst: No custom edge-case arguments have been calibrated for this C guest
