# Copyright (c) Meta Platforms, Inc. and affiliates.
# All rights reserved.
#
# This source code is licensed under the BSD-style license found in the
# LICENSE file in the root directory of this source tree.
#
# Generated migration inventory for C guests. See README.md for schema v2.

schema = 2
bucket = "c-programs"

[[test]]
id = "c-programs/acct-refusal-probe"
description = "Strict verification for tests/c/acct_refusal_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/acct_refusal_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/add-key-enosys"
description = "Strict verification for tests/c/add_key_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/add_key_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/adjtimex-deterministic"
description = "Strict verification for tests/c/adjtimex_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/adjtimex_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/arch-prctl-determinism"
description = "Strict verification for tests/c/arch_prctl_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/arch_prctl_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/bpf-enosys"
description = "Strict verification for tests/c/bpf_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/bpf_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/cachestat-enosys"
description = "Strict verification for tests/c/cachestat_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/cachestat_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/clock-adjtime-deterministic"
description = "Strict verification for tests/c/clock_adjtime_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/clock_adjtime_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/clone"
description = "Strict verification for tests/c/clone.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/clone.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/copy-file-range-refusal-probe"
description = "Strict verification for tests/c/copy_file_range_refusal_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/copy_file_range_refusal_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-copied-tiocgpgrp"
description = "Strict verification for tests/c/dbi_copied_tiocgpgrp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_copied_tiocgpgrp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-exec-failure"
description = "Strict verification for tests/c/dbi_exec_failure.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_exec_failure.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-execveat-unsupported"
description = "Strict verification for tests/c/dbi_execveat_unsupported.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_execveat_unsupported.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-mmap-exec"
description = "Strict verification for tests/c/dbi_mmap_exec.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_mmap_exec.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-pid-virtualization"
description = "Strict verification for tests/c/dbi_pid_virtualization.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_pid_virtualization.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-prlimit-self"
description = "Strict verification for tests/c/dbi_prlimit_self.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_prlimit_self.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-self-sigqueue"
description = "Strict verification for tests/c/dbi_self_sigqueue.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_self_sigqueue.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-unsupported-syscall"
description = "Strict verification for tests/c/dbi_unsupported_syscall.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_unsupported_syscall.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/dbi-wait-lifecycle"
description = "Strict verification for tests/c/dbi_wait_lifecycle.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/dbi_wait_lifecycle.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/epoll-determinism"
description = "Strict verification for tests/c/epoll_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/epoll_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/fp-reduction-nondeterminism"
description = "Strict verification for tests/c/fp_reduction_nondeterminism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/fp_reduction_nondeterminism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-fopenmp"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/futex-requeue-enosys"
description = "Strict verification for tests/c/futex_requeue_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/futex_requeue_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/futex-waitv-enosys"
description = "Strict verification for tests/c/futex_waitv_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/futex_waitv_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/futex-wake-enosys"
description = "Strict verification for tests/c/futex_wake_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/futex_wake_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/getcpu"
description = "Strict verification for tests/c/getCpu.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/getCpu.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/get-robust-list-child"
description = "Strict verification for tests/c/get_robust_list_child.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/get_robust_list_child.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/get-robust-list-self"
description = "Strict verification for tests/c/get_robust_list_self.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/get_robust_list_self.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/get-robust-list-thread"
description = "Strict verification for tests/c/get_robust_list_thread.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/get_robust_list_thread.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/getitimer-determinism-probe"
description = "Strict verification for tests/c/getitimer_determinism_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/getitimer_determinism_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/getpid"
description = "Strict verification for tests/c/getpid.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/getpid.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/getsockopt-null"
description = "Strict verification for tests/c/getsockopt_null.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/getsockopt_null.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/hello-alarm"
description = "Strict verification for tests/c/hello_alarm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/hello_alarm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/hello-signals"
description = "Strict verification for tests/c/hello_signals.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/hello_signals.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/io-uring-fallback"
description = "Strict verification for tests/c/io_uring_fallback.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/io_uring_fallback.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/io-uring-ring-determinism"
description = "Strict verification for tests/c/io_uring_ring_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/io_uring_ring_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ioctl-fioclex"
description = "Strict verification for tests/c/ioctl_fioclex.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ioctl_fioclex.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ioctl-siocethtool"
description = "Strict verification for tests/c/ioctl_siocethtool.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ioctl_siocethtool.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ipc-determinism"
description = "Strict verification for tests/c/ipc_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ipc_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/just-spin"
description = "Strict verification for tests/c/just_spin.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/just_spin.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/kcmp-eperm"
description = "Strict verification for tests/c/kcmp_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/kcmp_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/keyctl-enosys"
description = "Strict verification for tests/c/keyctl_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/keyctl_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/keyctl-passthrough"
description = "Strict verification for tests/c/keyctl_passthrough.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/keyctl_passthrough.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/listmount-enosys"
description = "Strict verification for tests/c/listmount_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/listmount_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/liteinst-advanced"
description = "Strict verification for tests/c/liteinst_advanced.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/liteinst_advanced.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/lsm-get-self-attr-enosys"
description = "Strict verification for tests/c/lsm_get_self_attr_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/lsm_get_self_attr_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/lsm-list-modules-enosys"
description = "Strict verification for tests/c/lsm_list_modules_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/lsm_list_modules_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/lsm-set-self-attr-enosys"
description = "Strict verification for tests/c/lsm_set_self_attr_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/lsm_set_self_attr_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/madvise-determinism"
description = "Strict verification for tests/c/madvise_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/madvise_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/map-shadow-stack-enosys"
description = "Strict verification for tests/c/map_shadow_stack_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/map_shadow_stack_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/memfd-secret-enosys"
description = "Strict verification for tests/c/memfd_secret_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/memfd_secret_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/meminfo-available-deterministic"
description = "Strict verification for tests/c/meminfo_available_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/meminfo_available_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/meminfo-cached-deterministic"
description = "Strict verification for tests/c/meminfo_cached_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/meminfo_cached_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/meminfo-free-deterministic"
description = "Strict verification for tests/c/meminfo_free_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/meminfo_free_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/memorypress"
description = "Strict verification for tests/c/memoryPress.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/memoryPress.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/mmap-determinism"
description = "Strict verification for tests/c/mmap_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/mmap_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/mmap-stress-determinism"
description = "Strict verification for tests/c/mmap_stress_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/mmap_stress_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/name-to-handle-at-enosys"
description = "Strict verification for tests/c/name_to_handle_at_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/name_to_handle_at_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/name-to-handle-directory-eopnotsupp"
description = "Strict verification for tests/c/name_to_handle_directory_eopnotsupp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/name_to_handle_directory_eopnotsupp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/name-to-handle-empty-path-eopnotsupp"
description = "Strict verification for tests/c/name_to_handle_empty_path_eopnotsupp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/name_to_handle_empty_path_eopnotsupp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/name-to-handle-regular-eopnotsupp"
description = "Strict verification for tests/c/name_to_handle_regular_eopnotsupp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/name_to_handle_regular_eopnotsupp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/nanosleep-par"
description = "Strict verification for tests/c/nanosleep-par.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/nanosleep-par.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netlink-autobind-generic"
description = "Strict verification for tests/c/netlink_autobind_generic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netlink_autobind_generic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netlink-autobind-route"
description = "Strict verification for tests/c/netlink_autobind_route.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netlink_autobind_route.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netlink-autobind-usersock"
description = "Strict verification for tests/c/netlink_autobind_usersock.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netlink_autobind_usersock.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netns-cookie-tcp4"
description = "Strict verification for tests/c/netns_cookie_tcp4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netns_cookie_tcp4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netns-cookie-tcp6"
description = "Strict verification for tests/c/netns_cookie_tcp6.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netns_cookie_tcp6.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/netns-cookie-udp4"
description = "Strict verification for tests/c/netns_cookie_udp4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/netns_cookie_udp4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/perf-event-hardware-enosys"
description = "Strict verification for tests/c/perf_event_hardware_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/perf_event_hardware_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/perf-event-open-eperm"
description = "Strict verification for tests/c/perf_event_open_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/perf_event_open_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/perf-event-software-enosys"
description = "Strict verification for tests/c/perf_event_software_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/perf_event_software_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/perf-event-watchpoint-enosys"
description = "Strict verification for tests/c/perf_event_watchpoint_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/perf_event_watchpoint_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/periodic-setitimer-delivery"
description = "Strict verification for tests/c/periodic_setitimer_delivery.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/periodic_setitimer_delivery.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pidfd-open-self"
description = "Strict verification for tests/c/pidfd_open_self.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/pidfd_open_self.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pidfd-poll-self"
description = "Strict verification for tests/c/pidfd_poll_self.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/pidfd_poll_self.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pidfd-waitid-child"
description = "Strict verification for tests/c/pidfd_waitid_child.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/pidfd_waitid_child.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ppoll-readv"
description = "Strict verification for tests/c/ppoll_readv.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ppoll_readv.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ppoll-simulation"
description = "Strict verification for tests/c/ppoll_simulation.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ppoll_simulation.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/prctl-dumpable"
description = "Strict verification for tests/c/prctl_dumpable.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/prctl_dumpable.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/print-memaddrs"
description = "Strict verification for tests/c/print_memaddrs.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/print_memaddrs.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/printf-with-threads"
description = "Strict verification for tests/c/printf_with_threads.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/printf_with_threads.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/proc-fd-link-aliases"
description = "Strict verification for tests/c/proc_fd_link_aliases.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/proc_fd_link_aliases.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/proc-fdinfo"
description = "Strict verification for tests/c/proc_fdinfo.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/proc_fdinfo.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-DFD_SOURCE=1"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/proc-locks"
description = "Strict verification for tests/c/proc_locks.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/proc_locks.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-DLOCK_API=1"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/process-mrelease-enosys"
description = "Strict verification for tests/c/process_mrelease_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/process_mrelease_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/process-vm-readv-refusal-probe"
description = "Strict verification for tests/c/process_vm_readv_refusal_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/process_vm_readv_refusal_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/process-vm-writev-refusal-probe"
description = "Strict verification for tests/c/process_vm_writev_refusal_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/process_vm_writev_refusal_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/procfs-positioned-probe"
description = "Strict verification for tests/c/procfs_positioned_probe.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/procfs_positioned_probe.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/prodcons-determinism"
description = "Strict verification for tests/c/prodcons_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/prodcons_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pselect6-simulation"
description = "Strict verification for tests/c/pselect6_simulation.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/pselect6_simulation.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ptrace-attach-eperm"
description = "Strict verification for tests/c/ptrace_attach_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ptrace_attach_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ptrace-eperm"
description = "Strict verification for tests/c/ptrace_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ptrace_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ptrace-seize-eperm"
description = "Strict verification for tests/c/ptrace_seize_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ptrace_seize_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ptrace-traceme-eperm"
description = "Strict verification for tests/c/ptrace_traceme_eperm.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ptrace_traceme_eperm.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pty-nr-count"
description = "Strict verification for tests/c/pty_nr_count.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/pty_nr_count.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/random-sources"
description = "Strict verification for tests/c/random_sources.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/random_sources.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/rcx-canonicalization"
description = "Strict verification for tests/c/rcx_canonicalization.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/rcx_canonicalization.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/record-replay-fd-close"
description = "Strict verification for tests/c/record_replay_fd_close.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/record_replay_fd_close.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/record-replay-file-state"
description = "Strict verification for tests/c/record_replay_file_state.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/record_replay_file_state.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/record-replay-lseek-seek-cur"
description = "Strict verification for tests/c/record_replay_lseek_seek_cur.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/record_replay_lseek_seek_cur.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/record-replay-setsockopt"
description = "Strict verification for tests/c/record_replay_setsockopt.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/record_replay_setsockopt.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/recvmsg-scm-rights-mmap"
description = "Strict verification for tests/c/recvmsg_scm_rights_mmap.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/recvmsg_scm_rights_mmap.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/remap-file-pages-anonymous-enosys"
description = "Strict verification for tests/c/remap_file_pages_anonymous_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/remap_file_pages_anonymous_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/remap-file-pages-memfd-enosys"
description = "Strict verification for tests/c/remap_file_pages_memfd_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/remap_file_pages_memfd_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/remap-file-pages-tmpfile-enosys"
description = "Strict verification for tests/c/remap_file_pages_tmpfile_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/remap_file_pages_tmpfile_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/request-key-enosys"
description = "Strict verification for tests/c/request_key_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/request_key_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/resource-determinism"
description = "Strict verification for tests/c/resource_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/resource_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sched-setattr-batch"
description = "Strict verification for tests/c/sched_setattr_batch.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sched_setattr_batch.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sched-setattr-idle"
description = "Strict verification for tests/c/sched_setattr_idle.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sched_setattr_idle.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sched-setattr-other"
description = "Strict verification for tests/c/sched_setattr_other.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sched_setattr_other.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sched-yield-progress"
description = "Strict verification for tests/c/sched_yield_progress.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sched_yield_progress.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/scheduler-policy-queries"
description = "Strict verification for tests/c/scheduler_policy_queries.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/scheduler_policy_queries.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/setitimer-determinism"
description = "Strict verification for tests/c/setitimer_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/setitimer_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sigmask-preemption"
description = "Strict verification for tests/c/sigmask_preemption.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sigmask_preemption.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/signal-determinism"
description = "Strict verification for tests/c/signal_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/signal_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sigpipe-siginfo"
description = "Strict verification for tests/c/sigpipe_siginfo.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sigpipe_siginfo.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sigtimedwait-no-timeout"
description = "Strict verification for tests/c/sigtimedwait-no-timeout.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sigtimedwait-no-timeout.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sigtimedwait-timeout-0s"
description = "Strict verification for tests/c/sigtimedwait-timeout-0s.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sigtimedwait-timeout-0s.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sigtimedwait-timeout-1s"
description = "Strict verification for tests/c/sigtimedwait-timeout-1s.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sigtimedwait-timeout-1s.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/hello-nostdlib"
description = "Strict verification for tests/c/simple/hello_nostdlib.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/simple/hello_nostdlib.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-std=gnu11", "-nostdlib", "-static", "-fno-stack-protector", "-fno-pie", "-no-pie"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/nanosleep-threads-nocrash"
description = "Strict verification for tests/c/simple/nanosleep-threads-nocrash.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/simple/nanosleep-threads-nocrash.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/nanosleep-threads-simple"
description = "Strict verification for tests/c/simple/nanosleep-threads-simple.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/simple/nanosleep-threads-simple.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/pread64-nostdlib"
description = "Strict verification for tests/c/simple/pread64_nostdlib.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/simple/pread64_nostdlib.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-std=gnu11", "-nostdlib", "-static", "-fno-stack-protector", "-fno-pie", "-no-pie"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/racewrite-nostdlib"
description = "Strict verification for tests/c/simple/racewrite_nostdlib.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/simple/racewrite_nostdlib.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare", "-std=gnu11", "-nostdlib", "-static", "-fno-stack-protector", "-fno-pie", "-no-pie"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/so-incoming-cpu-tcp4"
description = "Strict verification for tests/c/so_incoming_cpu_tcp4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/so_incoming_cpu_tcp4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/so-incoming-cpu-tcp6"
description = "Strict verification for tests/c/so_incoming_cpu_tcp6.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/so_incoming_cpu_tcp6.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/so-incoming-cpu-udp4"
description = "Strict verification for tests/c/so_incoming_cpu_udp4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/so_incoming_cpu_udp4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-cookie-tcp"
description = "Strict verification for tests/c/socket_cookie_tcp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_cookie_tcp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-cookie-udp"
description = "Strict verification for tests/c/socket_cookie_udp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_cookie_udp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-cookie-unix"
description = "Strict verification for tests/c/socket_cookie_unix.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_cookie_unix.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-ioctl-timestamp"
description = "Strict verification for tests/c/socket_ioctl_timestamp.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_ioctl_timestamp.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-timestamp-edge-cases"
description = "Strict verification for tests/c/socket_timestamp_edge_cases.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_timestamp_edge_cases.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-timestamp-timespec"
description = "Strict verification for tests/c/socket_timestamp_timespec.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_timestamp_timespec.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/socket-timestamp-timeval"
description = "Strict verification for tests/c/socket_timestamp_timeval.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/socket_timestamp_timeval.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/splice-enosys"
description = "Strict verification for tests/c/splice_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/splice_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/statmount-enosys"
description = "Strict verification for tests/c/statmount_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/statmount_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/syscall-file-io"
description = "Strict verification for tests/c/syscall_file_io.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/syscall_file_io.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/syscall-file-metadata"
description = "Strict verification for tests/c/syscall_file_metadata.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/syscall_file_metadata.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/syscall-quick-wins"
description = "Strict verification for tests/c/syscall_quick_wins.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/syscall_quick_wins.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sysfs-enosys"
description = "Strict verification for tests/c/sysfs_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sysfs_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sysinfo"
description = "Strict verification for tests/c/sysinfo.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sysinfo.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sysinfo-uptime"
description = "Strict verification for tests/c/sysinfo_uptime.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sysinfo_uptime.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/syslog-deterministic"
description = "Strict verification for tests/c/syslog_deterministic.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/syslog_deterministic.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sysv-sem-enosys"
description = "Strict verification for tests/c/sysv_sem_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sysv_sem_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/sysv-shm-enosys"
description = "Strict verification for tests/c/sysv_shm_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/sysv_shm_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/tcp-info-accept4"
description = "Strict verification for tests/c/tcp_info_accept4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/tcp_info_accept4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/tcp-info-accept6"
description = "Strict verification for tests/c/tcp_info_accept6.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/tcp_info_accept6.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/tcp-info-client4"
description = "Strict verification for tests/c/tcp_info_client4.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/tcp_info_client4.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/tee-enosys"
description = "Strict verification for tests/c/tee_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/tee_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/threadexhaustion"
description = "Strict verification for tests/c/threadExhaustion.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/threadExhaustion.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/thread-self-procfs-handoff"
description = "Strict verification for tests/c/thread_self_procfs_handoff.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/thread_self_procfs_handoff.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/thread-sync-determinism"
description = "Strict verification for tests/c/thread_sync_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/thread_sync_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/timer-create-determinism"
description = "Strict verification for tests/c/timer_create_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/timer_create_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/uname"
description = "Strict verification for tests/c/uname.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/uname.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/unix-autobind-dgram"
description = "Strict verification for tests/c/unix_autobind_dgram.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/unix_autobind_dgram.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/unix-autobind-seqpacket"
description = "Strict verification for tests/c/unix_autobind_seqpacket.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/unix_autobind_seqpacket.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/unix-autobind-stream"
description = "Strict verification for tests/c/unix_autobind_stream.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/unix_autobind_stream.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/ustat-enosys"
description = "Strict verification for tests/c/ustat_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/ustat_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/vforkexec"
description = "Strict verification for tests/c/vforkExec.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/vforkExec.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

  [test.build]
  cflags = ["-D_GNU_SOURCE", "-pthread", "-Wno-error=unused-parameter", "-Wno-error=unused-function", "-Wno-error=unused-variable", "-Wno-error=attributes", "-Wno-error=unknown-pragmas", "-Wno-error=sign-compare"]

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/vmsplice-enosys"
description = "Strict verification for tests/c/vmsplice_enosys.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/vmsplice_enosys.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/wait-on-child"
description = "Strict verification for tests/c/wait_on_child.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/wait_on_child.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"

[[test]]
id = "c-programs/writev-determinism"
description = "Strict verification for tests/c/writev_determinism.c"
lane = "portable"
requires = ["linux", "x86_64", "userns", "ptrace", "cc"]
timeout_seconds = 90
occasional = false
program = "tests/c/writev_determinism.c"
observation = { status = true, stdout = true, stderr = false, artifacts = [] }

[test.modes.verify]
backends_enabled = ["ptrace"]
[test.modes.verify.backends_disabled]
dbi = "Initial C-corpus migration preserves the established ptrace baseline; qualify DBI separately"
kvm = "Initial C-corpus migration preserves the established ptrace baseline; qualify KVM separately"
sabre = "Initial C-corpus migration preserves the established ptrace baseline; qualify SaBRe separately"
liteinst = "Initial C-corpus migration preserves the established ptrace baseline; qualify LiteInst separately"

[test.disabled_modes]
naked = "This migration inventories strict verification; it does not assert native nondeterminism"
replay = "Record/replay qualification is separate from the initial strict-verification migration"
chaos = "Chaos scheduling is only meaningful for guests with an explicit schedule-diversity oracle"
