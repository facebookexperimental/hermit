schema: 3
bucket: language-runtimes
test:
  - id: language-runtimes/bash-loop-pipe-time
    description: Bash file I/O, loops, pipelines, hashing, and time repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - bash
    occasional: false
    program: tests/e2e/language-runtimes/bash-loop-pipe-time.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: This regression establishes Hermit verification, not native nondeterminism
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: DBT Bash qualification is tracked by backend compatibility
          kvm: KVM interpreter coverage requires separate privileged qualification
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Bash qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Bash workload has no schedule-diversity oracle
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/bash-random
    description: Bash $RANDOM varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - bash
    occasional: false
    program: tests/e2e/language-runtimes/bash-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Bash qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Bash qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Bash workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/cpp-stl-determinism
    description: C++17 STL containers over std::random_device entropy, file I/O, and std::chrono repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cxx
    occasional: false
    program: tests/e2e/language-runtimes/cpp-stl-determinism.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT C++ qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst C++ qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The C++ workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/example-python-random
    description: The user-facing Python random example repeats deterministically under each applicable backend
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    direct:
      - ./examples/rand.py
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: The existing python-random test owns the native-variation assertion
      verify:
        ci:
          ptrace: true
          dbt: false
          kvm: true
          sabre: false
          liteinst: true
        backends_enabled:
          - ptrace
          - dbt
          - kvm
          - sabre
          - liteinst
        backends_disabled: {}
        ci_disabled_reason:
          dbt: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained a determinism failure; this cell did not pass every repetition cleanly. Retained detail: canonical verification did not match: verified=false verdict=diverged bitwise_parity=false", "result": "determinism-failure"}
          sabre: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained a determinism failure; this cell did not pass every repetition cleanly. Retained detail: canonical verification did not match: verified=false verdict=diverged bitwise_parity=false", "result": "determinism-failure"}
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Strict verification owns the examples-directory parity contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The example is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/gawk-random
    description: GNU awk rand() output varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - gawk
    occasional: false
    program: tests/e2e/language-runtimes/gawk-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT awk support is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst awk support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The awk workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/m4-macro-mkstemp
    description: m4 mkstemp temp-name varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - m4
    occasional: false
    program: tests/e2e/language-runtimes/m4-macro-mkstemp.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT m4 qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst m4 qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The m4 workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/perl-io-subprocess-time
    description: Perl file I/O, subprocess pipes, hashing, and time repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - perl
    occasional: false
    program: tests/e2e/language-runtimes/perl-io-subprocess-time.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: This regression establishes Hermit verification, not native nondeterminism
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Perl qualification is tracked by backend compatibility
          sabre: SaBRe path ineligible - the measured verify pair recorded 132 trusted shared-object sites (33 libc/ld-linux syscall instructions x Perl and tr x two runs; clock_gettime, getrandom, file I/O, memory mapping, and signal operations); forcing the first through the SaBRe marker caused SIGILL, so qualification requires zero trusted or fallback sites
          liteinst: LiteInst Perl qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Perl workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/perl-random
    description: Perl rand output varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - perl
    occasional: false
    program: tests/e2e/language-runtimes/perl-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Perl support is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Perl support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Perl PRNG probe is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/python-hash-determinism
    description: Python hash-seed order varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    program: tests/e2e/language-runtimes/python-hash-determinism.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Python support is tracked by backend compatibility
          sabre: SaBRe and ptrace each reach L2, but consume different deterministic entropy positions at Python exec and produce different hash seeds and stdout
          liteinst: LiteInst Python support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Python example is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/python-io-subprocess-time
    description: Python file I/O, subprocess capture, hashing, and time repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    program: tests/e2e/language-runtimes/python-io-subprocess-time.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: This regression establishes Hermit verification, not native nondeterminism
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Python qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Python qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Python workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/python-random
    description: Python random output varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    program: tests/e2e/language-runtimes/python-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        assert:
          bitwise_parity: true
        backends_disabled:
          dbt: DBT Python support is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Python support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Python example is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/rust-hashmap-iteration
    description: Rust HashMap/HashSet iteration order varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - rustc
    occasional: false
    program: tests/e2e/language-runtimes/rust-hashmap-iteration.rs
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Rust qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Rust qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Rust workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/ruby-random
    description: Ruby random output varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - ruby
    occasional: false
    program: tests/e2e/language-runtimes/ruby-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci:
          ptrace: true
          kvm: false
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Ruby support is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Ruby support is tracked by backend compatibility
        ci_disabled_reason:
          kvm:
            result: timeout
            evidence: KVM next-40 qualification at Hermit 4d8f866102882b6eeabdf99cc7e81433cc3c95c5; all-results.jsonl sha256 a14c6a90d249389336b894087fe16f56970298ca04620a1aadf7e94186e016ce
            reason: All three canonical repetitions exceeded the 57-second wall backstop on both attempts and produced no comparison, so KVM remains enabled but unselected.
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Ruby example is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/perl-hash-order
    description: Perl hash key iteration order varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - perl
    occasional: false
    program: tests/e2e/language-runtimes/perl-hash-order.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT perl support is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst perl support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The perl workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/lua-random
    description: Lua 5.4 math.random varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - lua5.4
    occasional: false
    program: tests/e2e/language-runtimes/lua-random.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: DBT Lua qualification is tracked by backend compatibility
          kvm: KVM interpreter coverage requires separate privileged qualification
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Lua qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Lua workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/python-hashseed
    description: Python string hash and set iteration order vary natively and repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    program: tests/e2e/language-runtimes/python-hashseed.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci:
          ptrace: false
        ci_disabled_reason:
          ptrace:
            result: determinism-failure
            evidence: "hermit#2384 8bc26dba1d53cbb8120db6f73456ad5ea788a8ca; task land-2384-io-buffer-hashing-first"
            reason: Io-buffer hashing is on by default from hermit#2384, so the canonical comparison now reads syscall output-buffer CONTENT that was previously invisible to it. This cell's first divergent record is an [iobuf] read of 10578 bytes whose length is identical across the pair while its hash differs, at record 3162. The divergence was always present; the comparison could not see it, and the green this replaces was false. This is NOT a flake and NOT a harness artefact - the two sibling cells that fail alongside it diverge on newfstatat directory metadata and are a different, unrelated cause.
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: DBT Python support is tracked by backend compatibility
          kvm: KVM Python coverage is in the privileged applications bucket
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Python support is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The hash-seed probe is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/python-dict-hash-iteration
    description: Python string-hash randomization (set/dict iteration order, hash(str)) plus file I/O and subprocess repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    program: tests/e2e/language-runtimes/python-dict-hash-iteration.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Native hash-seed variation is host/PYTHONHASHSEED sensitive; this regression establishes Hermit verification, not native nondeterminism
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Python qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Python qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Python workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/tcl-rand-clock
    description: Tcl rand()/clock output varies natively and repeats under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - tclsh
    occasional: false
    program: tests/e2e/language-runtimes/tcl-rand-clock.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci:
          ptrace: true
          kvm: true
        backends_enabled:
          - ptrace
          - kvm
        backends_disabled:
          dbt: DBT Tcl qualification is tracked by backend compatibility
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst Tcl qualification is tracked by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The Tcl workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: language-runtimes/node-v8-jit
    description: Node.js V8 JIT-compiles a hot loop and Math.random varies natively; both repeat under Hermit
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - node
    occasional: false
    program: tests/e2e/language-runtimes/node-v8-jit.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        runs: 3
        assert:
          min_distinct: 2
        backends_enabled:
          - native
        backends_disabled: {}
        ci_disabled_reason: This native control is not selected by ordinary validation; add it to ci/expected-e2e-plan.json only after repeated runs satisfy its declared outcome oracle.
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: DBI rewrites the code stream, so a JIT that writes its own code at run time needs separate qualification
          kvm: KVM requires the privileged runner
          sabre: SaBRe requires its external runtime
          liteinst: LiteInst patches the code stream, so JIT-generated code needs separate qualification
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The focused C sentinel owns the blocking replay contract
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: V8's background compiler and GC threads are not guest-visible concurrency the chaos scheduler can steer
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
