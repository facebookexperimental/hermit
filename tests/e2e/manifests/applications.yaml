schema: 3
bucket: applications
test:
  - id: applications/timed-progress-bar
    description: A time-driven Python application completes identically under verification
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    direct:
      - ./examples/timed-progress-bar.py
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Elapsed time drives progress but final output is intentionally stable
      verify:
        ci: true
        timeout_seconds:
          ptrace: 91
          kvm: 80
        cpu_timeout_seconds:
          ptrace: 32
          kvm: 24
        slow_reason:
          ptrace: Across 60 retained typed passing rows, nearest-rank p90 was 22.531 wall seconds and 20.710660 CPU seconds; ceil(4x wall)=91 and ceil(1.5x CPU)=32.
          kvm: The KVM-only workload runs 53 busy-wait and print intervals at 5 ms instead of 50 at 20 ms, reducing VM-exit-heavy clock reads while preserving the baseline's 200-turn instrumented scheduler coverage. Across 3 clean qualification repetitions at Hermit f190205a7b3e65e7ebf347ba0875b668fd72d9a7, nearest-rank p90 was 13.753 wall seconds and 12.809181 CPU seconds; ceil(4x wall)=56 and ceil(1.5x CPU)=20, both covered by the retained 80/24 bounds.
        backends_enabled:
          - ptrace
          - kvm
        guest_args:
          kvm:
            - "53"
            - "5"
        backends_disabled:
          dbt: DBT application coverage is owned by backend parity
          sabre: SaBRe does not yet determinize busy-wait polling on virtual wall-clock advancement; main validation has observed verify failure under load
          liteinst: LiteInst application coverage is owned by backend compatibility
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Focused time and entropy sentinels own the blocking replay contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The application is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: applications/example-timed-progress-bar
    description: The user-facing timed progress example repeats deterministically under each applicable backend
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - python3
    occasional: false
    direct:
      - ./examples/timed-progress-bar.py
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Elapsed time changes, but the final progress-bar output is intentionally stable
      verify:
        ci:
          ptrace: false
          dbt: false
          kvm: false
          liteinst: false
        backends_enabled:
          - ptrace
          - dbt
          - kvm
          - liteinst
        backends_disabled:
          sabre: SaBRe does not yet determinize busy-wait polling on virtual wall-clock advancement; keep the identical example workload unqualified
        ci_disabled_reason:
          ptrace: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained an understood infrastructure failure; this cell did not pass every repetition cleanly. Retained detail: verification recorded no comparison at all (verdict=no_result), so there is no canonical INFO evidence to admit", "result": "infrastructure-error"}
          kvm: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained an understood infrastructure failure; this cell did not pass every repetition cleanly. Retained detail: verification recorded no comparison at all (verdict=no_result), so there is no canonical INFO evidence to admit", "result": "infrastructure-error"}
          liteinst: {"evidence": "TaskGraph list-the-never-measured-red-cells-by-test-id-mode-backend; Hermit 5b811757980b9cab599206c047560a7962176a6b; repeated canonical verification summary sha256 944bf8af55c1102757a068818e840ee7f8e083f9855f9e302b1eddfd363e4cb4", "reason": "Repeated canonical verification retained an understood infrastructure failure; this cell did not pass every repetition cleanly. Retained detail: verification recorded no comparison at all (verdict=no_result), so there is no canonical INFO evidence to admit", "result": "infrastructure-error"}
          dbt:
            result: infrastructure-error
            evidence: RUN1598 at Hermit cd976f804de7568e34dc731b859dd7a5a1c1c0b9
            reason: Canonical full validation could not start either DBT attempt because this backend cannot isolate the required /test workdir; this is an infrastructure result, not a product divergence.
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Strict verification owns the examples-directory parity contract
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The example is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: applications/kvm-python-examples
    description: User-facing Python examples run identically with the system interpreter under KVM
    lane: privileged
    requires:
      - linux
      - x86_64
      - kvm
      - python3
    occasional: false
    program: tests/e2e/applications/kvm-python-examples.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: The portable Python-random test owns the native nondeterminism control
      verify:
        ci:
          kvm: false
        timeout_seconds:
          kvm: 74
        cpu_timeout_seconds:
          kvm: 25
        slow_reason:
          kvm: Across 58 retained typed passing rows, nearest-rank p90 was 18.271 wall seconds and 16.656796 CPU seconds; ceil(4x wall)=74 and ceil(1.5x CPU)=25.
        backends_enabled:
          - kvm
        backends_disabled:
          ptrace: This test specifically asserts the KVM execution path
          dbt: This test specifically asserts the KVM execution path
          sabre: This test specifically asserts the KVM execution path
          liteinst: This test specifically asserts the KVM execution path
        ci_disabled_reason:
          kvm:
            result: determinism-failure
            evidence: RUN1709 at Hermit 8c2bb5620f13e5d351e7bdb5d20c8dcec1f3eaba
            reason: Both canonical full-validation attempts diverged at turn 34, record 560, syscall 239, so KVM remains enabled but is not selected by CI pending separate qualification.
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: This is a KVM integration test
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The examples are single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: applications/kvm-shell-environment
    description: KVM executes a shell workload with a deterministic environment
    lane: privileged
    requires:
      - linux
      - x86_64
      - kvm
    occasional: false
    program: tests/e2e/applications/kvm-shell-environment.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: This is a privileged KVM backend integration test
      verify:
        ci: true
        backends_enabled:
          - kvm
        backends_disabled:
          ptrace: This test specifically asserts the KVM execution path
          dbt: This test specifically asserts the KVM execution path
          sabre: This test specifically asserts the KVM execution path
          liteinst: This test specifically asserts the KVM execution path
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: This is a KVM integration test
          dbt: Record/replay is unsupported by DBT
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The workload is single-threaded
          dbt: Chaos scheduling is unsupported by DBT
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: applications/c-toolchain-workflow
    description: Compile two C translation units, archive one, link them, and execute the result
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - cc
      - ar
    occasional: false
    program: tests/e2e/applications/c-toolchain-workflow.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: The fixed sources, compiler flags, and input make the toolchain transaction deterministic by construction
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: Establish the shared C toolchain transaction on the ptrace reference path before backend ratchets
          kvm: KVM requires the privileged runner and must ratchet after the ptrace reference path
          sabre: SaBRe multi-exec toolchain qualification must ratchet after the ptrace reference path
          liteinst: LiteInst multi-exec toolchain qualification must ratchet after the ptrace reference path
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Strict verification owns this toolchain transaction's blocking determinism contract
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The build graph is intentionally serial
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: No edge-case Hermit arguments are required
          dbt: No edge-case Hermit arguments are required
          kvm: No edge-case Hermit arguments are required
          sabre: No edge-case Hermit arguments are required
          liteinst: No edge-case Hermit arguments are required
  - id: applications/git-repository-workflow
    description: Create two Git commits, verify the repository, and archive the resulting tree
    lane: portable
    requires:
      - linux
      - x86_64
      - userns
      - ptrace
      - git
    occasional: false
    program: tests/e2e/applications/git-repository-workflow.sh
    observation:
      status: true
      stdout: true
      stderr: false
      artifacts: []
    modes:
      naked:
        ci: false
        backends_enabled: []
        backends_disabled:
          native: Fixed content, identities, timestamps, and configuration make the repository transaction deterministic by construction
      verify:
        ci: true
        backends_enabled:
          - ptrace
        backends_disabled:
          dbt: Establish the shared Git transaction on the ptrace reference path before backend ratchets
          kvm: KVM requires the privileged runner and must ratchet after the ptrace reference path
          sabre: SaBRe multi-exec Git qualification must ratchet after the ptrace reference path
          liteinst: LiteInst multi-exec Git qualification must ratchet after the ptrace reference path
      replay:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Strict verification owns this repository transaction's blocking determinism contract
          dbt: Record/replay is unsupported by DBI
          kvm: Record/replay is unsupported by KVM
          sabre: Record/replay is unsupported by SaBRe
          liteinst: Record/replay is unsupported by LiteInst
      chaos:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: The repository transaction is intentionally serial
          dbt: Chaos scheduling is unsupported by DBI
          kvm: Chaos scheduling is unsupported by KVM
          sabre: Chaos scheduling is unsupported by SaBRe
          liteinst: Chaos scheduling is unsupported by LiteInst
      custom:
        ci: false
        backends_enabled: []
        backends_disabled:
          ptrace: Strict verification owns the Git transaction; no custom Hermit arguments are required
          dbt: The Git transaction is not qualified on DBI
          kvm: The Git transaction is not qualified on KVM
          sabre: The Git transaction is not qualified on SaBRe
          liteinst: The Git transaction is not qualified on LiteInst
