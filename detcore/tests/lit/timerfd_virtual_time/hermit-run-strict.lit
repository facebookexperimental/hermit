# timerfd_settime now arms the timer against Detcore's virtual clock, so the
# blocking read completes at the guest's virtual deadline (elapsed virtual
# CLOCK_MONOTONIC >= the requested interval) rather than on host wall-clock.
# The XFAIL marker was removed when that virtual-time implementation landed.
RUN: %hermit run --strict -- %me
