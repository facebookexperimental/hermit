# timerfd_settime currently arms a host-kernel timer while CLOCK_MONOTONIC is
# virtualized. The blocking read therefore completes after host time passes,
# before the guest's virtual deadline. A timerfd virtual-time implementation
# should turn this XPASS, at which point this marker must be removed.
XFAIL: *
RUN: %hermit run --strict -- %me
