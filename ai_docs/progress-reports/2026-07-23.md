# Hermit Progress Report - 2026-07-23

This report measures the fork's `main` branch at
`a85f015b5607c2a08ec5b8fb19dcebf00ac18d91`. It uses fresh commands run on
2026-07-23 UTC. Results from open pull requests are excluded from every count.

## Snapshot

| Item | Value |
|---|---|
| Commit | `a85f015b5607c2a08ec5b8fb19dcebf00ac18d91` |
| Measurement window | 2026-07-23 15:00-15:35 UTC |
| Host | AMD EPYC 9D85 158-Core Processor |
| Kernel | `6.17.13-0_fbk0_crackerjackhost_0_g2b4321c50d79` |
| `perf_event_paranoid` | `1` |
| Rust | `rustc 1.99.0-nightly (be8e82435 2026-07-11)` |
| Cargo / nextest | `cargo 1.99.0-nightly`; `cargo-nextest 0.9.140` |
| Hermit binary | debug build from the measured commit |
| Run context | ptrace unless a DBI/KVM column says otherwise; default log level; no determinism relaxations |

The working tree was rebuilt after main advanced during the first measurement
attempt. All results below are from `a85f015`; earlier measurements were
discarded.

## Coverage Slope

The same five probes show the current progression from the leading mode to the
trailing backends:

| Mode | Fresh coverage | First support loss | Current limiting layer |
|---|---:|---|---|
| `run --strict --verify` | **5/5 PASS L2** | None in this probe set | Broadest supported mode |
| Record/replay `--verify` | **4/5 PASS R/R** | Concurrent shell pipeline | Replay output/trace mismatch |
| DBI | **0/5 runnable; backend blocked** | Backend preflight | External DynamoRIO SDK, `drrun`, and client artifact are not configured by main |
| KVM | **0/5 runnable; backend blocked** | Backend execution | VM initializes, but Linux ELF/VM/guest-kernel personality is absent |

This is the core status: **5/5 -> 4/5 -> blocked -> blocked**. Record/replay
retains single-process coverage but loses the concurrent pipeline. DBI and KVM
do not reach guest execution in the measured main environment.

## App Matrix

| Program / workload | Strict verify | Record/replay | DBI | KVM |
|---|---|---|---|---|
| `/bin/echo hello` | PASS L2 | PASS R/R | BLOCKED | BLOCKED |
| `sha256sum /etc/hostname` | PASS L2 | PASS R/R | BLOCKED* | BLOCKED* |
| Python `sum(range(100))` | PASS L2 | PASS R/R | BLOCKED* | BLOCKED* |
| Bash `echo hello \| wc -c` | PASS L2 | **FAIL**: recording/replay output and detlog mismatch | BLOCKED* | BLOCKED* |
| `gcc --version` | PASS L2 | PASS R/R | BLOCKED* | BLOCKED* |

`BLOCKED*` means the backend-wide gate was measured with `/bin/echo`; it occurs
before program-specific execution, so the other programs were not redundantly
launched.

### Strict / Verify

Command shape:

```bash
./target/debug/hermit run --strict --verify -- PROGRAM ARGS...
```

All five commands exited 0 with `Success: deterministic. Determinism verified.`
The pipeline is a real two-child live pipe and is the main discriminator from
record/replay in this sample.

### Record / Replay

Command shape:

```bash
./target/debug/hermit record start --verify --record-timeout 90 \
  --data-dir /tmp/UNIQUE_DIR -- PROGRAM ARGS...
```

Echo, sha256sum, Python, and `gcc --version` exited 0 with `Success: replay
matched recording.` The Bash pipeline exited 1 with `Mismatch between run 1
and run 2 outputs (logs retained)` and multiple detlog differences. Main
contains the internal-pipe classification and recorded-read-data changes, but
this workload still does not round-trip.

### DBI

With no environment configuration, the echo probe exited 1:

```text
backend `dbi` is unavailable: the DynamoRIO SDK was not found; set
DYNAMORIO_HOME or DynamoRIO_DIR to a valid SDK
```

Pointing only `DYNAMORIO_HOME` at a locally installed SDK advanced to a second
main-branch gate: `HERMIT_DRRUN` and `HERMIT_DBI_CLIENT` were required. No
unlanded client artifact was used. Therefore this report makes no DBI app or
determinism claim.

### KVM

`/dev/kvm` was readable and writable. The echo probe reached the KVM dispatch
and exited 1 with the main branch's fail-closed error:

```text
the KVM backend cannot run `/bin/echo`: reverie-kvm initialized a VM but does
not yet implement the Linux execution personality (ELF loader, virtual memory,
and guest-kernel ABI) required to execute a guest program
```

The KVM CLI path is wired, but no real app executed.

## Repository Health

| Command | Result |
|---|---|
| `cargo build -p hermit` | PASS, 47.96 s after the main update |
| `cargo test` | **INCOMPLETE** after a 15-minute bound |
| isolated `default_cargo_pipe_basics` | **TIMEOUT**, did not complete within 150 s |
| `cargo test -p hermit --test record_replay -- --test-threads=1` | PASS: 24 passed, 0 failed, 0 ignored, 46.09 s |
| `validate.sh --envelope-only` | PASS: L1/L2/L3/L4/RR each 3/3; L4 used 20 repetitions |
| full `validate.sh` | **INCOMPLETE**, externally interrupted by SIGTERM |
| GitHub main CI | Docs passed; Rust queued at the observation time; no green claim |

### Cargo Test Detail

The bounded `cargo test` run emitted complete summaries for 31 test binaries:
246 passed, 0 failed, and 13 ignored. It then remained in the 75-test
`hermit_modes` binary. `default_cargo_pipe_basics` was reported failed after
running for more than 60 seconds, while many sibling tests also exceeded 60
seconds. The run was interrupted after 15 minutes and produced no workspace
summary. An isolated, single-threaded retry of `default_cargo_pipe_basics` did
not complete within 150 seconds. Main is therefore **not proven Cargo-green**.

The previously failing absolute `FUTEX_WAIT_BITSET` test passed in this run;
the main-branch futex classification fix is present.

### Validate Detail

The full gate completed its first five checks:

- cargo-nextest available
- workspace build
- Hermit run smoke
- output determinism smoke
- verify-mode smoke

During `Test workspace and integrations`, nextest reported 166 passes before
the process received SIGTERM while `hermit_modes::chaos_mode_matrix` was
running; 125 of 292 selected tests were not run. The source of SIGTERM was not
established. No validation summary was produced, so this is an interruption,
not a green or product-failure verdict.

The separate envelope-only path completed at the exact SHA:

```text
L1 3/3  L2 3/3  L3 3/3  L4 3/3  rr 3/3
```

The three envelope probes are `true`, `echo`, and `date`; this small green
vector does not replace the five-app matrix or the incomplete full gates.

## Gaps And Next Actions

1. **R/R pipeline fidelity**: add the measured `echo | wc` workload as a
   regression and fix remaining replay output/trace ordering.
2. **DBI usability**: make main provide or reproducibly build the matching
   DynamoRIO client and launcher configuration, then rerun the identical five
   probes before claiming app support.
3. **KVM execution**: land a main-compatible Linux ELF/VM/guest ABI path, then
   run `/bin/echo` before expanding coverage or determinism claims.
4. **Cargo liveness**: diagnose `default_cargo_pipe_basics` exceeding 150
   seconds in isolation, then rerun the unbounded workspace suite.
5. **Validation isolation**: rerun full `validate.sh` without concurrent
   process cleanup or host interruption and require its final summary before
   calling main green.

## Notable Unlanded Work

These items are excluded from all measurements above:

- [Hermit #231](https://github.com/rrnewton/hermit/pull/231): draft KVM
  Detcore-over-loaded-ELF proof.
- [Reverie #41](https://github.com/rrnewton/reverie/pull/41): draft generic DBI
  `Tool<T>` runtime seam.
- [Reverie #42](https://github.com/rrnewton/reverie/pull/42) and
  [#44](https://github.com/rrnewton/reverie/pull/44): draft KVM static-ELF and
  ELF-through-Tool stack.
