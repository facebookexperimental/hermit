#!/bin/sh

mount -t proc none /proc 2>/dev/null
mount -t sysfs none /sys 2>/dev/null
mount -t devtmpfs none /dev 2>/dev/null || mount -t tmpfs none /dev 2>/dev/null
[ -c /dev/ttyS0 ] || mknod /dev/ttyS0 c 4 64 2>/dev/null

echo 'HERMIT-QEMU-BUSYBOX-START'
busybox uname -a
echo '--- root filesystem ---'
busybox ls -1 /
echo '--- four-stage pipeline ---'
busybox printf 'delta\nalpha\ncharlie\nbravo\n' | busybox sort | busybox head -n 3 | busybox sha256sum
echo '--- pi (bc -l, scale=10) ---'
echo 'scale=10;4*a(1)' | busybox bc -l
echo '--- busybox sha256 ---'
busybox sha256sum /bin/busybox
echo 'HERMIT-QEMU-BUSYBOX-PASS'
poweroff -f
