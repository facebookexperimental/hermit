name: CI (GitHub-hosted)

on:
  push:
  # Keep stacked pull requests covered before they are retargeted to main.
  pull_request:
  workflow_dispatch:

# Hosted capacity is elastic. Do not add a concurrency group here: every push
# and pull-request run should finish even while the self-hosted queue backs up.
env:
  CARGO_TERM_COLOR: always
  NEXTEST_VERSION: "0.9.100"
  RUSTFLAGS: -D warnings -C link-arg=-llzma
  RUSTDOCFLAGS: -D warnings
  DETCORE_TEST_RUST_LOG: "off"
  STRICT_COMPAT_HERMIT_BIN: ${{ github.workspace }}/target/debug/hermit

jobs:
  regular:
    name: Regular tests (GitHub-hosted)
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: stable
        cache: false
    - name: Enforce detcore backend-abstraction commandment
      run: ./scripts/check-detcore-backend-abstraction.sh
    - name: Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc binutils bsdextrautils bzip2 clang cmake cpio curl dc default-jdk-headless file g++ gcc gawk git gzip iproute2 jq less libunwind-dev liblzma-dev libxml2-utils lsof lua5.4 m4 make ncal netcat-openbsd nginx nodejs openssl patch perl php-cli pkg-config procps psmisc python3-full redis-server redis-tools ruby socat sqlite3 tcl time unzip util-linux vim-common wget xz-utils zip zlib1g-dev zstd
    - name: Enable unprivileged user and mount namespaces
      run: |
        if [[ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]]; then
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        fi
        unshare --user --map-root-user --pid --fork --uts --net --mount sh -c 'mount -t proc proc /proc && mount -t sysfs sysfs /sys && test -r /proc/self/status && test -r /sys/devices/system/cpu/online'
    - name: Install cargo-nextest
      run: cargo install cargo-nextest --locked --version "$NEXTEST_VERSION"
    - name: Rust components
      run: rustup component add clippy rustfmt
    - name: Portable test lane (no PMU or CPUID interception)
      run: ./validate.sh --hosted-only --no-label-pr --verbose
    # AUTONOMOUS-BOT-IMPLEMENTED
    # TODO-HUMAN-REVIEW(#712): Review bounded CI-only routing for no-PMU hangs.
    - name: Post-fork scheduling diagnostics (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 3
      run: cargo test -p detcore --test tests_misc ordinary_clone_ -- --test-threads=1
    - name: Network syscall determinism diagnostic (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 3
      run: cargo test -p detcore --test tests_misc network_syscalls_are_deterministic_across_five_runs -- --exact --test-threads=1
    - name: IPC determinism diagnostic (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 3
      run: cargo test -p hermit --test ipc_determinism ipc_patterns_are_deterministic_across_five_runs -- --exact --test-threads=1
    - name: Random-source determinism diagnostic (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 3
      run: cargo test -p hermit --test random_determinism random_sources_repeat_across_runs_and_change_with_seed -- --exact --test-threads=1
    - name: Threaded integration matrix diagnostic (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 5
      run: cargo test -p hermit --test integration_matrix -- --test-threads=1
    # The python3 --verify LiteInst cases observe interpreter-startup syscall
    # reordering under the non-determinizing preload backend, so they run as
    # bounded, observable diagnostics rather than blocking the no-PMU gate.
    - name: LiteInst python3 --verify diagnostics (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 5
      run: cargo test -p hermit --test cli -- run_liteinst_rejects_non_fork_clone run_liteinst_handles_inherited_ignored_sigchld run_liteinst_verifies_forked_guest run_liteinst_verifies_raw_fork_guest --test-threads=1
    - name: Chaos hello-race verification diagnostic (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 5
      run: cargo test -p hermit --test hermit_modes hello_race_chaos_verify -- --exact --test-threads=1
    # AUTONOMOUS-BOT-IMPLEMENTED
    # TODO-HUMAN-REVIEW(#598)
    - name: DBI pipe backpressure diagnostics (nonblocking)
      if: ${{ !cancelled() }}
      continue-on-error: true
      timeout-minutes: 5
      run: cargo test -p hermit --test cli run_dbi_verifies_pipe_backpressure -- --exact --test-threads=1
    - name: Upload nextest JUnit report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: nextest-junit-${{ github.job }}
        path: target/nextest/ci/junit.xml
        if-no-files-found: warn
        retention-days: 14
